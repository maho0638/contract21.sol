# contract21.sol
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

// Base network greeting smart contract
contract Contract21 {
    function greet() public pure returns (string memory) {
        return "Hello from Web3 on Base network";
    }
}
